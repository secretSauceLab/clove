services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: advocacy
      POSTGRES_USER: advocacy
      POSTGRES_PASSWORD: advocacy
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: